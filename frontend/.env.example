# Base URL of the StackScout Express Backend API
VITE_API_BASE=http://localhost:3001

# Set to true to run the frontend completely offline in self-contained mock mode (using simulated memory stores)
VITE_MOCK=false
